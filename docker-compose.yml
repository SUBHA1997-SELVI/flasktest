version: "3.9"
services:
  web:
    build: .
    ports:
      - "8000:5000"
  redis:
    image: "redis:alpine"
    
    
    # run in your project directory 
    
 docker-compose up
 
 # Enter http://localhost:8000/ in a browser to see the application running.
